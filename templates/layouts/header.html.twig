<div class="container">
    <nav class="navbar navbar-expand-md navbar-light">
      <div class="navbar-collapse collapse w-25 order-1 order-md-0 dual-collapse2">
        <a class="navbar-brand" href="{{ path('homepage') }}">AmaShopping</a>
      </div>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02"
        aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
  
      <div class="navbar-collapse collapse w-100 dual-collapse2">
        <ul class="navbar-nav nav navbar-righ ml-auto">
          {% for category in categoryRepository.findAll() %}
          <li class="nav-item">
            <a class="nav-link" href="{{path('product_category', {slug: category.slug})}}">{{category.name}}</a>
            </a>
          </li>
        {% endfor %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('products')}}">Products
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{path('create_product')}}">Create Product</a>
          </li>
        </ul>
        <ul class="navbar-nav">
          {% if app.user %}
          <li class="nav-item m-1">
            <a href="{{path('security_logout')}}" class="btn btn-outline-danger">Logout</a>
          </li>
          {% else %}
          <li class="nav-item m-1">
            <a href="{{path('security_login')}}" class="btn btn-success">Login</a>
          </li>
          {% endif %}
        </ul>
      </div>
    </nav>
  </div>